#include<bits/stdc++.h>
using namespace std;

//Find the Median of two sored arrays in O(log n)

int findMedian(vector<int> arr1, vector<int> arr2) {
    //not yet done
}

int main() {
    vector<int> arr1 = {1, 4, 6, 7, 8, 10};
    vector<int> arr2 = {2, 3, 9, 12, 24, 25, 27}
    cout<<findMedian(arr1, arr2);
}